<!doctype html>
<html>
	<head>
		<meta charset="utf-8" />
		<meta http-equiv='X-UA-Compatible' content='IE=edge;chrome=1' />

		<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
		<meta name="viewport" content="width=device-width">

		<!-- Use title if it's in the page YAML frontmatter -->
		<title><%= data.page.title || "The Middleman" %></title>
		<link href='http://fonts.googleapis.com/css?family=Source+Sans+Pro:400,200,200italic,400italic,700' rel='stylesheet' type='text/css'>
		<%= stylesheet_link_tag "normalize", "all" %>

		<script src="http://code.jquery.com/jquery-1.10.1.min.js"></script>
		<%= javascript_include_tag  "underscore" %>
		<%= javascript_include_tag  "all" %>
	</head>
	<body>

		<div class="page">

			<header>
				<nav>
					<a href="/" id="logo">no<span>..</span><small>way</small></a>
				</nav>

				<aside>
					<ol>
						<li><a href="/blog">blog</a></li>
						<% blog.tags.each do |tag, articles| %>
							<li class="tag-name"><%= link_to tag, tag_path(tag) %></a></li>
						<% end %>
						<% blog.articles.group_by {|a| a.date.year }.each do |year, articles| %>
							<li class="tag-name"><%= link_to year, blog_year_path(year) %></a></li>
						<% end %>
					</ol>
				</aside>
			</header>

			<section id="main" role="main">
				<%= yield %>
			</section>

			<%= partial "footer" %>
		</div>
	</body>
</html>
